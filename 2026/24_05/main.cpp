#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    int maxJumps(vector<int>& arr, int d)
    {
        // Jump game problem solution goes here!
    }
};

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    return 0;
}